# ============================================================
# Business Central API — Required Secrets / Environment Variables
# ============================================================
#
# IMPORTANT FOR AI TOOLS (Lovable, Bolt, etc.):
# Do NOT hardcode these values. Instead:
#   - In Lovable: Add each variable below as a Secret
#     (Settings → Secrets → Add Secret)
#   - In Bolt: Add as Environment Variables
#   - In local dev: Copy this file to .env and fill in values
#
# This file is a REFERENCE for variable names and descriptions.
# The actual values must be provided via platform secrets or a
# local .env file that is never committed to git.
# ============================================================

# ------------------------------------------------------------
# AUTHENTICATION — OAuth2 Client Credentials
# ------------------------------------------------------------

# Azure / Entra ID Tenant ID
# Where to find it: Azure Portal > Entra ID > Overview > Tenant ID
# Lovable secret name: BC_TENANT_ID
BC_TENANT_ID=

# App Registration Client ID
# Where to find it: Azure Portal > Entra ID > App Registrations > Your App > Application (client) ID
# Lovable secret name: BC_CLIENT_ID
BC_CLIENT_ID=

# App Registration Client Secret
# Where to find it: Azure Portal > Entra ID > App Registrations > Your App > Certificates & Secrets > New client secret
# Lovable secret name: BC_CLIENT_SECRET
BC_CLIENT_SECRET=

# Token URL — do not change this unless using a sovereign cloud
BC_TOKEN_URL=https://login.microsoftonline.com/${BC_TENANT_ID}/oauth2/v2.0/token

# Scope — do not change this
BC_SCOPE=https://api.businesscentral.dynamics.com/.default


# ------------------------------------------------------------
# BUSINESS CENTRAL ENVIRONMENT
# ------------------------------------------------------------

# Environment name — usually "Production" or "Sandbox"
# Lovable secret name: BC_ENVIRONMENT
BC_ENVIRONMENT=

# Base URL for all standard API calls
# This is constructed from your Tenant ID and Environment name
BC_BASE_URL=https://api.businesscentral.dynamics.com/v2.0/${BC_TENANT_ID}/${BC_ENVIRONMENT}/api/v2.0

# Company ID — the GUID of the BC company you want to work with
# How to find it: Call GET {{BC_BASE_URL}}/companies after auth is set up, copy the "id" field
# Lovable secret name: BC_COMPANY_ID
BC_COMPANY_ID=

# Human-readable company name — used for display only
# Lovable secret name: BC_COMPANY_NAME
BC_COMPANY_NAME=


# ------------------------------------------------------------
# AUTOMATION API
# ------------------------------------------------------------

# Base URL for the Automation API (separate from standard API)
BC_AUTOMATION_URL=https://api.businesscentral.dynamics.com/v2.0/${BC_TENANT_ID}/${BC_ENVIRONMENT}/api/microsoft/automation/v2.0


# ------------------------------------------------------------
# ON-PREMISES ONLY — ignore if using BC Online / SaaS
# ------------------------------------------------------------

# BC_ONPREM_BASE_URL=https://your-server:7048/BC/api/v2.0
# BC_ONPREM_USERNAME=your-username
# BC_ONPREM_PASSWORD=your-password


# ------------------------------------------------------------
# OPTIONAL SETTINGS
# ------------------------------------------------------------

# Default language for API responses
BC_LANGUAGE=en-US

# Default page size for list requests (max is 20000)
BC_DEFAULT_PAGE_SIZE=100
